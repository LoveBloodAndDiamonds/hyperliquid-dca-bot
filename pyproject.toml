[project]
name = "hyperliquid-dca-bot"
version = "0.1.0"
description = "Cron-based DCA bot for Hyperliquid spot market"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "unicex>=0.28.1",
    "loguru>=0.7",
    "python-dotenv>=1.0",
    "aiohttp>=3.9",
]
